"""Where each plugin's data comes from, one module per source.

Keeping fetching and parsing here leaves a plugin file as the menu it
describes: a few components and the call that configures them.
"""
